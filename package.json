{
  "scripts": {
    "cypress": "$(npm bin)/cypress",
    "wait-on": "$(npm bin)/wait-on",
    "prettier": "$(npm bin)/prettier"
  },
  "dependencies": {
    "cypress": "^4.1.0",
    "prettier": "^2.0.1",
    "wait-on": "^4.0.1"
  }
}
